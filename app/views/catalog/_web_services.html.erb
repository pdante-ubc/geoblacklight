<% document ||= @document %>

<% document.distributions.service_distributions.each do |reference| %>
  <% if Settings.WEBSERVICES_SHOWN.include? reference.type.to_s %>
    <%= render_web_services(reference) %>
    <hr>
  <% end %>
<% end %>
